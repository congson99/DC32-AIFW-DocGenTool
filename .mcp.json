{
  "mcpServers": {
    "atlassian": {
      "command": "npx",
      "args": ["-y", "@bodywave/jira-mcp@1.1.1"],
      "env": {
        "JIRA_URL": "${JIRA_URL}",
        "JIRA_EMAIL": "${JIRA_EMAIL}",
        "JIRA_API_KEY": "${JIRA_API_KEY}"
      }
    }
  }
}
